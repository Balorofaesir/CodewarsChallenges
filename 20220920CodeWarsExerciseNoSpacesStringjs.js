let Str1 = "Hola Q Hace";

const RemoveSpaceFromStrings = (parameter) => {
  let StringWhithOutSpaces = parameter.replace(/ /g, "");

  return StringWhithOutSpaces;
};

//I did not got the result because i changed the name of the 
//function and also used the arrow method.

//function noSpace(parameter){
//  let StringWhithOutSpaces = parameter.replace(/ /g, "");

//  return StringWhithOutSpaces;
//};
